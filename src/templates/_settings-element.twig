{% import "_includes/forms" as forms %}

{{ forms.checkboxSelectField({
  label:         '{elementName} sources'|t('_typedlinkfield', { 'elementName': linkType.displayName }),
  instructions:  'Select the available sources.'|t('_typedlinkfield'),
  id:            'sources',
  name:          'sources',
  options:       linkType.availableSources,
  values:        sources,
  showAllOption: true,
})}}

<fieldset>
  {{ forms.checkboxField({
    label:   'Allow users to enter custom queries that will be appended to the url'|t('_typedlinkfield'),
    name:    'allowCustomQuery',
    id:      'allowCustomQuery',
    name:    'allowCustomQuery',
    checked: linkType.allowCustomQuery
  }) }}

  {{ forms.checkboxField({
    label:   'Allow links to reference a different site'|t('_typedlinkfield'),
    name:    'allowCrossSiteLink',
    id:      'allowCrossSiteLink',
    name:    'allowCrossSiteLink',
    checked: linkType.allowCrossSiteLink
  }) }}
</fieldset>
